# Model routing log

Observational log feeding the "Model routing" rules in `CLAUDE.md`. After each routed
task (subagent dispatch or notable inline task), append a row. Periodically (every ~15-20
rows) distill patterns back into the CLAUDE.md rules and prune distilled rows.

**Outcome codes:** `pass` = accepted first try; `rework` = needed a correction round;
`fail` = redone at a higher tier. Tokens = output tokens if known (from
`node scripts/usage-report.mjs`), else blank.

| Date | Task (short) | Type | Model / effort | Outcome | Tokens | Notes |
|------|--------------|------|----------------|---------|--------|-------|
| 2026-07-02 | Money-path review (3 parallel read-only reviewers: edge fns, RLS, client state) | review | fable / default | pass | ~360k (subagents) | 5 CRITICAL + 8 HIGH real findings; every controller spot-check confirmed; ~1 false-positive-risk finding (H4 retry) needed call-site verification. Fable-tier review of money code: worth it. |
| 2026-07-02 | Security hardening Phase 1 (6 DB migrations + verify script) | implement | sonnet / default | rework | ~184k (subagent) | Enumeration + SQL was strong, BUT controller review caught 2 real defects before push: a 2-step staging bypass of the reg-paid trigger (both signal columns client-writable) and a PUBLIC-grant no-op in the coupon revoke. Lesson: money/auth DB triggers WRITTEN by sonnet still need fable-tier review of the SQL before applying — the enumeration is delegable, the invariant-adversarial check is not. |
| 2026-07-02 | Cart-state fixes (8 tasks: C5/H5–H8/M6–M9, client) | implement | sonnet / default | rework | ~248k (subagent) | Structure/tests strong (20-case pure-fn suite), but controller review caught 2 real MONEY bugs in the draft: H6 "clear the hold" mirrored the payment-SUCCESS path → marked an unpaid membership ACTIVE (free membership) because membershipHolds derives active from holds; and H7 charged full entry within a $0-change window (over-charge). Same lesson as Phase 1: Sonnet is good at the mechanical/structural fix + tests; the money-invariant adversarial read is the controller's job. Pattern holding across 2 tasks — bake into routing: money/auth/cart implement → sonnet draft, ALWAYS fable-review the diff before merge. |
| 2026-07-02 | Security hardening Phase 2 (edge fns: C4/H4/H1/M5 + snapshot migration) | implement | fable / default | pass | (controller-implemented) | DELIBERATE exception to "sonnet drafts": the two edge functions ARE the live money path, the pieces interlocked subtly (reg-state pricing + ownership + TOCTOU snapshot + fulfillment reorder), and correctness dominated token cost — controller implemented directly with full context loaded. Key design win found while reading the code: the snapshot SUBSUMES H1 (removes fulfillment's cart_items dependency → claim can move to the end → retryable in TS), avoiding the plan's risky plpgsql port. Verified live (create side 5/5 + webhook 2xx). Lesson: for the highest-risk money path where a brief would be nearly as long as the code, controller-implement + self-review can beat delegate+review. |
| 2026-07-04 | B4 "Meet management" (4 sub-items: event Draft/Live phase derivation, edit-lockout trigger, club-transfer roster-visibility fix, synchro same-level RPC) | implement | controller (self) / default, opus-effort adversarial review each sub-item | rework (1 of 4) | ~330k across the 4 sub-items + reviews | Controller-implemented directly (not sonnet-dispatched) for all 4 — each touched RLS/triggers/a new RPC. Sub-item 4 (synchro-level-sync RPC) is the standout data point: adversarial review caught a CRITICAL fail-open auth bug in a NEW SECURITY DEFINER function the controller itself wrote — `if not (is_admin() or athlete_id = my_person_id() or manages_club(club_id))` copied an RLS policy's boolean expression but not its fail-CLOSED semantics; for an anon caller the OR-chain evaluates to Postgres NULL (not false), so `if not NULL` silently did NOT raise, letting an unauthenticated caller push a level change onto an arbitrary registration's partner via the SECURITY DEFINER bypass. Fixed with `coalesce(..., false)`. **Confirms the existing rule needs no carve-out for controller-authored code**: "always adversarially review money/auth/RLS before merge" applies even when the controller (not sonnet) wrote the SQL — self-review during writing did NOT catch this; a dedicated adversarial-review pass, reasoning through the anon/NULL case specifically, did. |

## Current priors (mirror of CLAUDE.md rules)
- haiku: mechanical + explicit verify checklist (renames, plumbing recipe, doc sweeps).
- sonnet: default implementer for well-specified tasks.
- opus/fable: design, decomposition, money/auth/RLS review, gnarly debugging.
- Subagent vs inline: subagent when the task reads many files the controller doesn't
  need; inline when spawn overhead (~full context re-read per agent) exceeds the work.
| 2026-07-04 | Admin.tsx (2578 ln) split into pages/admin/* barrel + 11 modules (pure code motion) | refactor | sonnet / default | pass | ~167k (subagent) | Clean first try. Only off-brief decision: kept `REGION_OPTIONS` module-private (react-refresh lint). Controller verified cheaply via sorted-line diff (only import-depth + export-keyword deltas) + build/lint/250 tests + live smoke of all 4 admin pages / 7 league tabs. Good routing fit: big mechanical motion = sonnet; the diff-based review made fable verification ~free. |
| 2026-07-04 | Staging standup (project create, 59 migrations, 15 fn deploys, secrets, Stripe webhook endpoint, seed + dev users) | ops/infra | controller (self) / default | pass | inline | Side-effect-heavy (CLI + live services) = controller work per standing rules. Found 2 real latent bugs: pushAll memberships onConflict matched a constraint dropped 20260618000007; seeded dev addon cart line violated S4's addon-needs-event rule. Also: fresh-backend boot-order gotcha documented (seasons before first sign-in). |
| 2026-07-04 | Playwright E2E harness + 5 smoke specs vs staging | implement | sonnet / default | pass | ~118k (subagent) | 4/5 green first try; the 5th failed on a REAL staging seed bug (not a spec bug) which the agent correctly flagged instead of masking — controller fixed seed + staging rows, then 5/5. Good judgment call; selector adaptations all reasonable (read Gate/Cart/Events sources). |
| 2026-07-06 | Codebase survey of current event-management coverage (14 areas) to ground the v2 requirements gap analysis | explore/survey | Explore subagent / inherited (fable) | pass | ~100k (subagent) | Accurate structured report on first try; 2 minor slips (called send-email "Gmail SMTP" — it's Resend; overstated refund-flow presence) caught by controller against CLAUDE.md before writing docs. Spec digestion + gap mapping itself stayed controller-inline (writing task, needed full context of Julia's doc). |
| 2026-07-06 | emv2 P0 Task 1: scheduler infra (pg_cron/pg_net + notification_log + scheduled-dispatch fn + sanction reminders) | implement (new infra) | sonnet / default | pass (1 fable fix) | ~123k (subagent) | Clean first try incl. a self-caught upsert/ignoreDuplicates API correction. Fable review added 1 real fix: release notification_log claims on failed Resend batch (transient outage otherwise permanently eats reminders). |
| 2026-07-06 | emv2 P0 Task 2: event field extensions (migration + types + wizard UI + sanction mapping) | implement (feature) | sonnet / default | pass (1 cosmetic fable fix) | ~189k (subagent) | Avoided the whole-row-upsert nulling trap unprompted; live responsive verify at 375/1280. Fable fix: degenerate '{ccOnConfirmation:false}' director backfill nulled out. |
| 2026-07-06 | emv2 P0 Task 3: late-reg fee pricing (money path) | implement (money) | sonnet draft / fable review | fail→fixed | ~177k (subagent) + controller fix | CONFIRMS the standing rule: sonnet's mechanics + tests were solid, but the once-per-athlete invariant was under-specified in the brief and the committed anchor rule double-charged repeat purchases / two-save carts. Fable adversarial read caught it; fix (lateFeeAnchor contains-earliest-reg rule) landed inline after subagent quota outage. |
| 2026-07-07 | emv2 P0 Task 3 fix + Task 4 (per-event confirmation email in stripe-webhook + resend reply_to/fromName) | implement (money-adjacent) | controller (fable) inline | pass | inline | Subagent pool hit the weekly limit mid-phase — controller finished inline. Not the preferred routing; acceptable for a contained, controller-designed fix. |
| 2026-07-08 | Rebrand T1: brand tokens + Supabase-hosted webfonts + all-caps display type | implement (UI/tokens) | sonnet / default | pass | ~115k (subagent) | Clean first try incl. self-run contrast audit; correctly flagged 2 pre-existing AA failures (badge.err, stat-accent) instead of silently fixing — controller applied the 2-line fix. |
| 2026-07-08 | Rebrand T2: real logo SVGs in chrome + favicon | implement (UI) | sonnet / default | pass | ~146k (subagent) | Self-caught a Vite base-path favicon 404 via preview_network before finishing. Flagged PWA PNG icons as follow-up per brief; controller regenerated via npx sharp-cli (SVG→PNG). |
| 2026-07-08 | Rebrand T3: MAG/WAG/TNT discipline icons on events surfaces | implement (UI) | sonnet / default | pass | ~121k (subagent) | Good taste judgment (skipped EventWizard clutter, said so). Live 375/1280 verify. |
| 2026-07-08 | Rebrand T4: exact palette in email-layout + fn inline styles + jsPDF | implement (colors-only) | sonnet / default | pass | ~109k (subagent) | Clean hex-map table returned; sensible judgment calls (kept neutral grays, kept waiver legal text grayscale). Controller reviewed diff (receipt-adjacent), deployed 11 fns with verify_jwt flags intact. |
| 2026-07-08 | Auth email templates unified with Resend layout (config.toml + render script + config push, prod) | ops/infra (auth config) | controller (fable) inline | pass (1 self-inflicted incident, recovered) | inline | Side-effect-heavy = controller per standing rules. INCIDENT: `supabase config push` auto-confirmed under agent detection AND pushed CLI defaults for undeclared [auth] keys — briefly reset prod site_url/redirects/confirmations; restored within minutes from the captured diff. Also found CLI 2.107 silently skips template content (fixed in 2.109) and staging free-tier 400s template pushes. E2E-verified via Gmail MCP (branded recovery email received; ~5 min content propagation). |
| 2026-07-09 | emv2 P1 T1: event owner + checklist (migration/RLS/RPC + UI + pure logic) | implement (feature + RLS) | sonnet / default | fail→pass (1 review round) | ~305k (subagent, incl fix) | Missed the documented RLS upsert trap (UPDATE-only policy can't satisfy pushEvent's upsert); controller fable review caught it, fix clean. |
| 2026-07-09 | emv2 P1 T2: owner-task escalation emails in scheduled-dispatch | implement (edge fn consumer) | sonnet / default | pass | ~99k (subagent) | Clean mirror of the sanction consumer; per-day idempotency keying right first try. |
| 2026-07-09 | emv2 P1 T3: event_admins grants (definer RPCs + capability threading + UI) | implement (auth/ACL) | sonnet / default | pass (session-limit restart) | ~285k (subagent, 2 runs) | Migration/RPCs passed fable review unchanged; uid-scoped capability threading (impersonation-safe) was its own good call. Cut off once by plan limit. |
| 2026-07-09 | emv2 P1 T4: event-files private bucket + storage policies + upload UI | implement (storage/RLS) | sonnet / default | pass | ~107k (subagent) | Policies passed fable review unchanged. |
| 2026-07-09 | emv2 P1 T5: host viewing page + 3 host RPCs (roster/money/medals) | implement (feature + money-adjacent SQL) | sonnet / default | fail→pass (1 review round) | ~355k (subagent, incl fix) | Solid page + money rationale, but hit the jsonb_set intermediate-key no-op (host "mark received" silently didn't persist); fable review caught it. Pattern holds: sonnet misses one SQL-semantics edge per money/auth task. |
| 2026-07-09 | emv2 P1 T6: Excel workbook exports (exceljs, pure sheet models) | implement (UI/export) | sonnet / default | pass | ~123k (subagent) | Good scope judgment (per-registration rows + deferred P2 sheets, reasoned from legacy masters); chunk isolation verified. |
| 2026-07-09 | emv2 P1 T7: send-event-email edge fn + EventCommunicate page | implement (auth edge fn) | sonnet / default | fail→pass (1 review round; controller finished 5-line fix after session-limit cutoff) | ~194k+ (subagent) | Auth/recipient resolution clean; missed cc-fan-out duplication (cc'd address gets one copy per recipient). |
| 2026-07-09 | emv2 P1 T8: host post-close edit (trigger GUC bypass + roster RPCs) | implement (money/auth critical) | sonnet / default | fail→pass (1 review round) | ~438k (subagent, incl fix) | Excellent trigger analysis (GUC confinement > blanket is_event_host bypass — its own reasoning); missed the club-membership-gate domain rule on the new registration entry point; fable review caught it. |
| 2026-07-10 | emv2 P2 T1: per-unit add-on fields migration + plumbing + config UI | implement (DB plumbing recipe) | sonnet / default | pass | ~122k (subagent) | Clean; also spotted the Sanction-approval leoAddon drop (fixed in T5). |
| 2026-07-10 | emv2 P2 T2: server add-on pricing, deadlines, banquet assignment | implement (money-critical) | sonnet / default | pass (fable review: no defects) | ~166k (subagent) | First money task where sonnet missed nothing; fail-closed repricing + H4-mirrored ownership both correct. Accepted gaps noted by controller (pending-payment assignment race → P4 soft-hold; no server addon_size presence check). |
| 2026-07-10 | emv2 P2 T3: individual per-unit add-on purchase UI | implement (UI) | sonnet / default | pass (rule violation) | ~239k (subagent) | Feature correct, but VIOLATED the no-side-effects rule: ran `supabase db push` to PROD itself mid-verification (additive columns, no harm; staging reconciled at phase end). Reinforce "no push/deploy" as a hard constraint in briefs. |
| 2026-07-10 | emv2 P2 T4: club Add-ons card | implement (UI) | sonnet / default | pass | ~178k (subagent) | Good reuse (extracted shared pickers); sourced max-1-assigned check from cart+invoices client-side. |
| 2026-07-10 | emv2 P2 T5: camp club-gate carve-out + overnight survey UI | implement (domain gate + UI) | sonnet / default | pass (fable review: no defects) | ~268k (subagent) | Carve-out correctly scoped to registration only (hosting gate untouched — its own judgment); flagged the unbuilt simplified camp popup as a spec gap. |
| 2026-07-10 | emv2 P2 T6: camp confirmation email in stripe-webhook | implement (webhook-adjacent) | sonnet / default | pass (fable review: no defects) | ~125k (subagent) | Fail-soft isolation + full HTML escaping of user text; corrected the brief's wrong route guess from App.tsx. |
| 2026-07-10 | emv2 P2 T7: leo/banquet/shirt export sheets + camp export + host RPCs | implement (feature + auth SQL) | sonnet / default | pass (fable review: no defects) | ~179k (subagent) | RPC auth predicate copied verbatim from P1 pattern, verified fail-closed (auth_has_role is exists()-based). |
| 2026-07-10 | emv2 P3 T1: event-edit persistence debug (datetime-local blanking + edit-button RLS gate) | debug | opus / default | pass | | 2 real root causes found in one pass: timestamptz→datetime-local conversion losing the value on load, and the Edit-event button not matching events RLS's admin/sanctioning gate (hosts silently rejected on save). |
| 2026-07-10 | emv2 P3 T2: normalizeExternalUrl applied to hotelLink + medal trackingLink | implement (small fix) | sonnet / default | pass | | Clean, mechanical. |
| 2026-07-10 | emv2 P3 T3: $0-total checkout (coupon-fully-covered cart skips Stripe, fulfillPayment extraction) | implement (money-critical) | sonnet draft / fable review | rework (1 fix) | | Sonnet's extraction + free-path wiring was structurally sound, but the first draft left a failed free-order fulfillment stranded (no retry/error surfacing) — controller review added inline retry + error_logs + non-500-stranding behavior. |
| 2026-07-10 | emv2 P3 T4: refund foundations (refund_manager role, refunds_foundations migration, is_league_host flag, RLS) | implement (migrations/RLS) | sonnet draft / fable review | rework (1 fix) | | Sonnet's schema/RLS design was sound, but `refund_requests.payment_id` was drafted as text — controller review caught the FK type mismatch (`payments.id` is the lone uuid PK in the schema) before `db push`. |
| 2026-07-10 | emv2 P3 T5: refund request flows (self-serve + club manager dialogs, request-refund edge fn) | implement (feature + auth edge fn) | sonnet draft / fable review | rework (1 fix) | | Auth/eligibility logic was solid, but `payments.ref_reg_ids` is never populated by create-checkout-session (reg refs live in lines_snapshot only) — the first draft's payment lookup via that column always came back empty; fixed to resolve via invoice_items instead. |
| 2026-07-10/11 | emv2 P3 T6: refund review page + process-refund (Stripe refund processing, review UI) | implement (money-critical) | sonnet draft / fable review | rework (1 fix + controller RLS gap) | | Sonnet fixed a coupon over-refund bug in review; controller's own self-found RLS read gap for refund_manager review access (T6.5, `20260710230000`) then caused a follow-on 42P17 RLS-recursion regression discovered live, requiring a same-day hotfix migration (`20260711023234`) — reinforces the "cross-table subquery in a policy" trap now documented in CLAUDE.md. |
| 2026-07-11 | emv2 P3 T7: refund receipts in Purchase History + post-refund UI states | implement (UI + PDF) | sonnet / default | pass + controller-added guard | | Feature shipped clean; controller added the DB-level `guard_registration_refunded` trigger afterward (post-T7 security review) since the "can't re-enable a refunded reg except admin" rule was UI-only and reachable via direct PostgREST. |
| 2026-07-11 | emv2 P3 docs sweep (supabase/README, CLAUDE.md, docs/README, model-routing-log, spec §H annotation) | docs | sonnet / default | pass | | Standard phase-close docs sweep. |
| 2026-07-13 | emv2 P4 T7: waitlist promotion sweep (scheduled-dispatch), manage-waitlist edge fn (promote/requeue/list), admin queue card, Complete-checkout flow, waitlistPosition helper | implement (service-role state transitions + emails + auth edge fn) | fable (direct implementer, session-limit restart mid-task) | pass (staging e2e verified: waiting→notified→promoted lifecycle, admin promote/requeue, 403s for non-admin) | | RLS SELECT on waitlist_groups only exposes a group to its own club/person, so queue visibility for hosts/sanctioning ships as a server-authorized `list` action on manage-waitlist rather than an RLS relaxation. Pre-existing 375px overflow (owner-checklist file input) flagged as a separate task. |
| 2026-07-13/16 | emv2 P5 A1–E1 (10 tasks): session-request surveys + checkout gate, competition order + drag UI, finals lineups + scheduler nag/lock + Telnyx extraction, nationals dashboard, check-in | implement (schema/RLS ×5, money-path gate, UI ×4, scheduler) | sonnet drafts / fable(→opus) controller reviews every diff | all 10 first-try DONE, zero rework | ~1.4M subagent tokens | Fable designed/decomposed + briefed with exact schemas/RLS templates; sonnet executed all 10 clean — the "rigor lives in the brief" rule held: naming the template migration + pasting exact predicates eliminated RLS drift. Money-path gate (A3) verified adversarially by controller: server-derived scope, fail-closed, 400 pre-Stripe. Session-limit interruption between D1 dispatches; work resumed losslessly from task list + commits. |
| 2026-07-16 | emv2 P6 T1: finance foundations migration (accounting_codes/host_payouts + finance_admin read RLS) + TS plumbing | implement (schema/RLS) | sonnet / default | pass first-try | ~128k subagent tokens | Brief pasted exact predicates + the four-separate-policies rule; sonnet also proactively verified the table-grant question (default Supabase privileges, documented in the migration). Fable-reviewed clean. |
| 2026-07-16 | emv2 P6 T2: pure finance engine (src/lib/finance.ts: txns/summary/date-defaults/sheets + linesSnapshot mapping) + 24 vitest tests | implement (money math, pure) | sonnet / default | pass first-try | ~146k subagent tokens | Semantics fully fixed in the brief (paid_cents precedence, fee proration example with exact value, UTC ranges); sonnet's judgment calls (UTC over machine-local) were correct and documented. Fable-reviewed the whole file line-by-line — no defects found. |
| 2026-07-16 | emv2 P6 T3: finance dashboard UI (#/admin/finance, 3 tabs, host payout ledger, xlsx export extraction) | implement (UI, multi-file) | sonnet / default | pass + 1 controller fix | ~165k subagent tokens | Only defect: copied the codebase's pre-existing undefined `var(--accent)` idiom (latent bug in 4 other pages, spun off as a task); controller swapped to `--teal-900` directly. Live prod functional sweep (payout add/persist/delete, code save/clear, click-through, 375/768/1280) all passed. |
| 2026-07-17 | Write-queue hardening: error classifier, permanent-failure rollback via drain-then-sync, transient budget 8×/~2min, read-only-offline gate + 33-file success-path sweep | implement (data-layer machinery, every write path) | sonnet / default | pass + 2 controller-review fixes (rollback-sync could clobber still-pending writes; false success toasts after blocked offline mutations) — same-agent rework fixed both | ~420k subagent tokens (incl. rework) | The two misses were both cross-cutting interaction bugs, not local defects — consistent with the "adversarial invariant read is not delegable" rule. Rework via SendMessage to the same agent was cheap and lossless. |
| 2026-07-17 | Report-a-problem widget + version stamp + report-problem edge fn (category-routed email, ring buffer, git-SHA define) | implement (feature, FE+edge fn) | sonnet / default | pass first-try; controller live-smoke-tested deployed fn (400 invalid / 200 send) | ~158k subagent tokens | Clean: proactive contrast probe, honest screenshot-tool-failure disclosure with substitute evidence. |
| 2026-07-17 | MFA A+B+C: TOTP UI, aal2 step-up interstitial, is_admin() hardening migration, admin-reset-mfa, passkey bridge; then AAL-guard sweep across 9 privileged edge fns | implement (auth/RLS — highest-risk class) | sonnet / default; fable controller adversarial review | pass + 1 real security gap caught in review (admin-reset-mfa lacked the aal2 check it exists to enforce — service-role functions bypass RLS-level is_admin) → same-agent fix + controller-approved 9-fn sweep | ~800k subagent tokens (3 rounds) | Sonnet's enumeration of the other 8 exposed functions (unprompted, with per-fn power analysis) was excellent; the miss was again the cross-layer invariant ("edge fns bypass the RLS hardening"), caught only by the fable review. Pattern holding: sonnet = mechanics + local reasoning; controller = invariants across layers. |
| 2026-07-17 | camp_survey privacy fix: scoped-read RPC + table-grant column swap + loadAll explicit columns + 2 modal prefill fetches | implement (auth/RLS) | sonnet / default; fable controller review + live probes | pass + 1 gap found by controller's staging anon-probe (RPC still executable by anon via Supabase default function privileges — data fail-closed, [] returned; fixed in follow-up migration 20260717211754) | ~212k subagent tokens | Sonnet independently caught the grant-union no-op trap on the TABLE side (excellent) but missed the same trap on the FUNCTION side — again the pattern: local reasoning strong, cross-layer invariant (default privileges) caught only by controller verification against the live DB. Probe-after-apply is now clearly mandatory for grant/revoke migrations. |
| 2026-07-17 | Daily "anything wrong?" digest (scheduled-dispatch kind, notification_log day-key dedupe, pure window logic + 16 tests) | implement (edge fn feature) | sonnet / default | pass first-try; controller review found nothing | ~147k subagent tokens | Clean; reused existing claim idiom correctly, verified the admin route instead of trusting the brief's guess ('/admin/errors' not '/admin/error-log'), did its own doc sweep. |
| 2026-07-18 | Passkey sign-in (free Passkeys API): Gate button, ProfilePasskeys card, dead MFA-webauthn UI removal, CDP virtual-authenticator e2e (skip-gated), config.toml passkey keys | implement (auth UI, no RLS) | sonnet / default; fable controller review | pass — review found no defects; SDK typing + config-key claims independently re-verified (experimental.passkey in auth-js dist; config.toml parsed by CLI) | ~242k subagent tokens | PROCESS MISS: agent merged+pushed to main itself despite "commit on the branch" brief — review happened post-push (no harm this time, but reiterate the stop-at-branch boundary explicitly in future auth briefs). Work itself was excellent: found registerPasskey's no-friendlyName gap and handled it, left staging clean, honest skip reporting. |
| 2026-07-18 | F2 component tests (jsdom+RTL setup + 17 tests: cart removal, change-fee, hold badges) | implement/tests | sonnet / default | pass | ~187k (subagent) | First-try green, evidence complete; self-diagnosed the RTL auto-cleanup-with-`globals:false` trap and the vitest-loads-.env.local inertness trap. Controller review found no defects, only a quality nit (test duplicated Cart.tsx's toast map — tautology risk), fixed by controller directly (export CART_REMOVAL_MESSAGE). Test-writing remains solidly sonnet-delegable. |
| 2026-07-18 | F3 focus-refresh (refetch-on-focus module + pure decision fn + 10 tests) | implement (client data-layer, small) | sonnet / default | pass | ~107k (subagent) | First-try; correctly derived the away/return event split the spec didn't spell out, reused getWriteQueueState instead of new plumbing, and verified the StripeCheckout non-interaction claim itself. Controller review found nothing to change. |
| 2026-07-18 | F4 payments reconciliation (reconcile-payments edge fn: scan/refulfill/mark-refunded ops, recon.ts pure drift classifier + 16 tests, Finance.tsx Reconciliation tab, recon_note migration drafted) | implement (money-critical: Stripe reads + fulfillPayment re-invocation + payments.status write) | sonnet / default | drafted on feat/payments-recon, STOPPED at branch per brief — pending controller fable-review before merge/push/db-push/deploy | not yet measured | Self-caught during the build (not by an external reviewer): the first refulfill draft called `fulfillPayment` directly, skipping the M5 Stripe-amount-vs-payment-row reconciliation assertion that stripe-webhook's own wrapper performs before calling the same core — added a matching check (+ a $0-total guard for the free-order branch) before considering the op done. Could not live-test the edge function (no deploy per brief); browser smoke-check of the new Finance tab was inconclusive (local dev-auth admin seed user didn't resolve `isAdmin`/`isFinanceAdmin` in this environment) — flagged for controller verification. Migration follows the established `_shared/X.ts` mirrors `src/lib/X.ts` pattern (like `_shared/stripe.ts`/`pricing.ts`) for the pure verdict logic. |
| 2026-07-18 | F5 admin data export + delete/anonymize (pure collector + completeness test, jsPDF/JSON export, admin-delete-person edge fn) | implement (auth + financial-integrity) | sonnet / default; fable controller review | pass — review's one suspicion (entry lines missing ref_user_id → labels escaping the scrub) disproven by checking all 8 line-construction sites; agent independently discovered the ON DELETE CASCADE hard-delete hazard and designed tombstone-in-place around it | ~220k (subagent) | Strongest sonnet showing yet on a high-risk task: the cascade discovery + waiver_signatures-as-blocking-ref call were exactly the cross-layer invariants that previously needed the controller. Controller still verified independently (staging smoke: hard-delete, tombstone, refusal, 403 — all correct first try). |
| 2026-07-18 | F6 season lifecycle (launched_at migration+backfill, pure nag/rollover logic + 23 boundary tests, EventWizard guard, checkout season gate, scheduled-dispatch consumer, Seasons launch UI, future-season membership UX) | implement (money + scheduler, multi-surface) | sonnet / default; fable controller review | pass — review's one flag (server gate accepts launched-PAST seasons) turned out to match deliberate client late-signup semantics, accepted; agent pre-empted the seasonForDate fallback trap, the two-currents-vs-zero-currents flip ordering, and honestly scoped out the cross-year nag edge | ~199k (subagent) | Live staging validation COMPLETE: real stale-current staging data exercised both paths — daily nag email fired on the first cron tick post-deploy, and after launching s27 the next tick auto-flipped current (one current, one-time rollover email logged). |
| 2026-07-19 | UI fix batch (drag-down snap-back → computeDropIndex + 8 tests, brand badge recolor + rebrand-spec pairings, admin profile breadcrumb/dual-role/current+future-seasons) | implement (UI, pure-logic fix) | sonnet / default | pass — first-try green (843 tests), enumerated all not-changed color pairings as briefed, correctly cleared FinalsLineupEditor of the same bug | ~154k (subagent) | Controller pre-diagnosed the drag bug + pre-verified contrast (7.5/8.8/4.8:1), which kept the brief tight. Sanction.tsx `var(--green)`/`var(--red)` undefined-token badge spotted in passing → fixed later same session. |
| 2026-07-19 | Roadmap B–E current-state audit | explore (read-only) | fable Explore / default | pass — correctly found B mostly shipped, C complete, D/E unbuilt; citations checked out during implementation | ~129k (subagent) | Audit-before-ask paid off: turned four vague roadmap letters into three sharp PM questions; C needed zero build. |
| 2026-07-19 | B residuals (athlete-gate canRegister + per-type admin grant/revoke) | implement (registration gating = money-adjacent) | sonnet / default; fable controller review | pass — review clean; agent found 2 latent bugs itself (coach-revoke deleting regs; Members-list revoke wiping both type rows) and confirmed the (person,season,type) PK before building | ~303k (subagent) | The consumer-by-consumer enumeration demanded by the brief is what surfaced the synchro-partner-list gap. react-hooks/purity false-positive on untouched Date.now() documented + scoped-disabled. |
| 2026-07-19 | D codeless judge access (judge_access_codes migration, judge-entry edge fn, public unlock routes, host QR card) | implement (anon-token → DB writes; security-critical) | sonnet / default; fable controller review | pass — review found 1 gap (unbounded source/calc/calcState on anonymous submits), fixed by controller directly + tests; rest of the surface held (id recomposition, cross-event confinement, RLS idiom, trio untouched) | ~253k (subagent) | Controller-authored security design in the brief (token model, service-role confinement, brute-force logging) meant sonnet had no security decisions to invent — the one miss was a resource-abuse cap, consistent with the known pattern: sonnet nails the enumerated mechanics, the adversarial-invariant read stays with the controller. |
| 2026-07-19 | E scoring config (scoring_config + deductions2/e_score2 migration, panels averaging, EventWizard section, Judge 2-panel UI, anon-path lockstep) + Sanction undefined-token badge fix | implement (scoring schema, no money) | sonnet / default; fable controller review | pass — killed mid-task by session limit, resumed via SendMessage with working tree intact and finished clean (889 tests); sensible deviation (no calcState2 → plain judge-2 numeric input) flagged not buried | ~232k+ (subagent, across both runs) | First real test of resume-same-agent-after-API-death: transcript resume + surviving working tree made the restart cheap. |
| 2026-07-22 | Event-detail page feedback (subtitle/UCG label/Participants/quick-links gating) | implement (display-only UI) | sonnet / default | pass — first-try green (970 tests), restructured links as computed array as briefed | ~85k (subagent) | Small precise brief from a prior Explore map; no rework. |
| 2026-07-22 | FlipFest wizard trims (12 field/section hides + template address/fee + camp auto-lockout) | implement (UI + template) | sonnet / default | pass — first-try green; independently caught that camps DO consume disciplines/sessions downstream and seeded the template instead of blanking (the brief demanded that check) | ~162k (subagent) | The "verify downstream consumers before removing data" clause in the brief did real work. |
| 2026-07-22 | Nationals full-page variant + create defaults + sessions×gyms model (pure builder + 6 tests) | implement (feature UI + pure logic) | sonnet / default; fable controller diff review | pass — first-try green (976 tests); self-caught a react-hooks/refs violation; controller review verified level ids + submit-path invariants, no defects found | ~212k (subagent) | Controller pre-designed the slots×gyms→EventSession mapping in the brief; sonnet executed cleanly incl. live dev-server responsive pass. |
| 2026-07-22 | Camps session-less/level-less (wizard, RegistrationEditor camp mode, display sweep) | implement (registration UI, fee-adjacent) | sonnet / default | pass — first-try green (980 tests incl. 4 new component tests); found 2 display warts via its own Explore sub-search; live end-to-end camp reg smoke incl. cleanup | ~204k (subagent) | Also flagged the RosterToolsCard camp gap honestly instead of scope-creeping (spun off as a task chip). |
| 2026-07-22 | Camp survey config + host review card (surveyMandatory, wizard section, response card) | implement (feature UI + pure validation) | sonnet / default | pass — first-try green (983 tests); respected the scoped-RPC privacy boundary; documented a react-hooks/purity lint quirk instead of hacking around it | ~208k (subagent) | Scout-first (Explore) meant the brief could say "the survey already exists" — the task shrank to config + one card. |
| 2026-07-22 | UCG host-club removal + refund eligibility + director/age-calc (money) | implement (money/refunds) | sonnet / default; fable controller review | pass — sonnet itself caught the `''==''` host-$0 bypass (unaffiliated camp reg = free entry) and guarded 5 functions; controller review found the remaining hole was ucg_hosted being client-writable → controller authored guard-trigger migration 20260722220449 | ~142k (subagent) | The pattern held again: sonnet nails enumerated mechanics + even one adversarial catch, but the privilege-escalation angle (who can WRITE the flag that grants eligibility) still came from the controller review. |
| 2026-07-22 | Two-tier Nationals publish (listing_only migration + wizard modes + listing gating) | implement (feature, new column) | sonnet / default; controller live re-verify | pass with 1 controller fix — sonnet caught its own session-wiping bug in dates-only re-publish, but stamped listingOnly:false on EVERY submit, breaking all event saves against pre-migration prod (silent write-queue retries); controller made the column write conditional + verified the queue drain live | ~256k (subagent) | Lesson: when adding a NOT-YET-APPLIED column to a whole-row-upsert table, the ship-window compat (omit-when-unset) must be in the brief explicitly — the smoke test even SHOWED the failure ("column not found") and the agent read it as expected-pending rather than a ship blocker. Also: dev smokes against prod leave failed writes QUEUED in localStorage — clean up `ucg.writeQueue.v1`, not just the visible event. |
| 2026-07-23 | Camp single-row/discipline-less registration + roster removal | implement (registration core) | sonnet / default | pass — first-try green (989 tests); handled legacy multi-row no-churn well; live smoke caught the environment-wide reg-write failure and correctly ruled OUT its own change via a control repro | ~292k (subagent) | The control-experiment instinct (reproduce on a non-camp event) is what turned "my change broke it?" into "pre-existing prod outage". |
| 2026-07-23 | URGENT: registrations upsert 42501 (camp_survey EXCLUDED-read privilege) + masked level_id FK | fix (prod outage, money-path adjacent) | controller diagnosed (error_logs + migration read), sonnet implemented | pass — live-verified 201/204 against prod; found the second (masked) FK bug during verification | ~246k (subagent) + controller diagnosis | Diagnosis chain: error_logs raw message ("permission denied for table") ≠ RLS wording → privilege layer → EXCLUDED-read rule. The 2026-07-17 migration's write-path comment was confidently wrong; trap now in CLAUDE.md. Merged+pushed IMMEDIATELY (partial batch) since prod was live-broken. |
| 2026-07-23 | Camp survey question builder (model+resolver+editor+5 consumer surfaces+edge fn) | implement (feature, cross client/edge) | sonnet / default | pass — first-try green (1001 tests); multi-camp-cart per-event question mapping handled unprompted; respected the fresh pushCampSurvey write-path invariant | ~274k (subagent) | Big-surface rewrite went clean because the brief enumerated all 11 consumers from the scout map. |
| 2026-07-23 | UCG event-page cleanup (owner/admins/status hides, summary card, edit-nav) | implement (UI) | sonnet / default | pass — first-try green; picked the existing event_host_addons RPC for add-on counts after checking its RLS allows admin/sanctioning | ~144k (subagent) | Cheap task; the "reuse the cheapest existing source, don't invent a fetch" clause did its job. |
